@model proekt_turizam.Models.SavedTour

@{
    ViewBag.Title = "Delete";
}

<div class="d-flex justify-content-center align-items-center" style="min-height: 90vh;">
    <div class="card shadow-lg" style="background-color: #F9F2E8; width: 50%; border-radius: 20px;">
        <div class="card-body">
            <h2 class="text-center mb-4 text-danger">Delete Saved Tour</h2>
            <h5 class="text-center mb-4">Are you sure you want to delete this saved tour?</h5>
            <hr />

            <dl class="row">
                <dt class="col-sm-4 fw-bold">@Html.DisplayNameFor(model => model.Tour.Title)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.Tour.Title)</dd>

                <dt class="col-sm-4 fw-bold">@Html.DisplayNameFor(model => model.SavedOn)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.SavedOn)</dd>
            </dl>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                <div class="text-center mt-4">
                    <input type="submit" value="Delete" class="btn btn-danger mx-2" />
                    <a href="@Url.Action("Index", "SavedTours")" class="btn btn-success mx-2">Back to List</a>
                </div>
            }
        </div>
    </div>
</div>
